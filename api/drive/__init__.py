"""Drive module — Google Drive read-only integration (SPEC-006 FA-14).

Phase 18 scaffolding: OAuth flow stubs + token persistence model.
Real OAuth credentials are NOT configured yet.
TODO: configure GOOGLE_OAUTH_CLIENT_ID and GOOGLE_OAUTH_CLIENT_SECRET in
      api/.env before any live OAuth handshake can complete.
"""
