# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# Claude Code per-user overrides — must NOT be committed.
# CLAUDE.md and .claude/settings.json are committed; *.local.* are personal.
CLAUDE.local.md
.claude/settings.local.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Python (api/ backend)
__pycache__/
*.py[cod]
*.egg-info/
api/.venv/
api/.ruff_cache/
api/.pytest_cache/
api/uv.lock
api/.env
api/mlruns/
firebase-debug.log

# Credenciais / segredos — NUNCA commitar
firebase-service-account.json
**/firebase-service-account.json
*-service-account*.json
**/application_default_credentials.json
docker-compose.prod-db.yml
