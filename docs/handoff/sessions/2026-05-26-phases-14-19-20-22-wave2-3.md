# Handoff — 2026-05-26 — Phases 14, 19, 20, 22 (Wave 2+3)

**Duração aproximada:** ~3h (sessão com context summary)
**Foco:** Implementar roadmap Wave 2+3 via team agents paralelos, resolver conflitos de merge, abrir PR #11

## O que foi feito

### Phase 14 — Onboarding / Empty States (completo)
- `components/solar/WelcomeScreen.tsx` — reescrito com overlay mode (`overlay?`, `onDismiss?`, `onOpenGuide?`), Carbon icons (Group, Star, Close, Help)
- `hooks/useFirstVisit.ts` — hydration-safe (starts null, reads localStorage `sunos:welcomed`)
- `components/onboarding/GettingStartedGuide.tsx` — modal 4 passos, Carbon icons
- `app/page.tsx` — integra `useFirstVisit` + `GettingStartedGuide`
- `app/agentes/page.tsx` — `EmptyState` com Carbon `Bot` icon

### Phase 19 — FR-185 Alerta 72h (completo)
- `lib/onboarding-types.ts` — `isEntityStale({ status, createdAt }, thresholdHours=72)` exportada
- `components/onboarding/EntityValidationCard.tsx` — prop `createdAt?`, badge `WarningAlt` (Carbon) quando estale
- `app/clientes/[clientId]/onboarding/validate/page.tsx` — `staleCount` + banner amarelo
- `contexts/OnboardingOraculoContext.tsx` — mock backdates 80h para demonstração

### Phase 20 — Aprovação Hierárquica (parcialmente completo)
- `lib/approval-types.ts` — campo `original_content?` em `ApprovalSubmission`
- `data/approvals-admin.ts` — mock `sub-003` com `original_content`
- `contexts/ApprovalsContext.tsx` — `pendingCount` exportado
- `components/layout/AppHeader.tsx` — notification bell (`Notification` Carbon) com badge vermelho
- `app/aprovacoes/[submissionId]/page.tsx` — painéis Antes/Depois side-by-side quando `original_content` existe
- `app/aprovacoes/page.tsx` — abas `Pendentes/Histórico` + agrupamento por `client_id`
- ⚠️ Pendente: notificações email/FCM (externo)

### Phase 22 — APScheduler (completo)
- `api/agents/scheduler.py` — `AsyncIOScheduler`, `register_agent_schedule`, `unregister_agent_schedule`, `load_active_schedules_from_store`, `run_scheduled_agent`
- `api/agents/schemas.py` — `AgentScheduleConfig` + `AgentScheduleOut`
- `api/agents/router.py` — `PATCH /{agent_id}/schedule` + `GET /{agent_id}/schedule`
- `api/main.py` — startup/shutdown do scheduler no lifespan (manteve preview cleanup loop)
- `api/pyproject.toml` — `apscheduler>=3.10.0`
- `api/tests/test_agents.py` — 18 novos testes
- ⚠️ Pendente: Cloud Scheduler/GCS (Fase D)

### ROADMAP
- `docs/ROADMAP.md` — Phase 14 marcada ✅, Phase 19 FR-185 ✅, Phase 20 histórico ✅, Phase 22 APScheduler ✅
- Tabela de mapeamento Fase de Produto ↔ Phases atualizada

## Decisões tomadas

- **Carbon icons everywhere**: qualquer agent que usasse lucide-react foi corrigido para @carbon/icons-react sem `strokeWidth`
- **Cherry-pick pattern**: agents comitaram em worktrees isoladas → cherry-pick para branch principal com resolução de conflito manual
- **APScheduler vs Cloud Scheduler**: implementado APScheduler in-process (Fase C); Cloud Scheduler deferred para Fase D (PRE-01/04)
- **Phase 20 notification**: badge no AppHeader suficiente para Fase A; email/FCM explicitamente deferred

## Arquivos modificados

**Frontend:**
- `components/solar/WelcomeScreen.tsx`
- `hooks/useFirstVisit.ts` (novo)
- `components/onboarding/GettingStartedGuide.tsx` (novo)
- `app/page.tsx`
- `app/agentes/page.tsx`
- `lib/onboarding-types.ts`
- `components/onboarding/EntityValidationCard.tsx`
- `app/clientes/[clientId]/onboarding/validate/page.tsx`
- `contexts/OnboardingOraculoContext.tsx`
- `lib/approval-types.ts`
- `data/approvals-admin.ts`
- `contexts/ApprovalsContext.tsx`
- `components/layout/AppHeader.tsx`
- `app/aprovacoes/[submissionId]/page.tsx`
- `app/aprovacoes/page.tsx`

**Backend:**
- `api/agents/scheduler.py` (novo)
- `api/agents/schemas.py`
- `api/agents/router.py`
- `api/main.py`
- `api/pyproject.toml`
- `api/tests/test_agents.py`

**Docs:**
- `docs/ROADMAP.md`

## Pendências (não abertas como TODO)

- Phase 18: sync incremental, ACL, job queue (Google Drive API — externo)
- Phase 19: Real Deep Agent (LangGraph, não mock)
- Phase 20: notificações push/email (email/FCM — externo)
- Phase 21: integração Oráculo FA-15 (depende de Phase 19 real agent)
- Phase 22: GCS memory files + Cloud Scheduler (Fase D)
- Phase 23: Firebase Admin SDK real (Fase B — hoje mock in-memory)
- Phase 16: bloqueado DEC-06

## Próximo passo natural

Mergear PR #11 → depois avaliar Phase 16 (aguarda DEC-06) ou aprofundar Fase D das phases existentes (GCS, Cloud Scheduler, Firebase Admin SDK real).

## Aprendizados / pegadinhas

- Agents paralelos com `isolation: "worktree"` frequentemente usam lucide-react — sempre verificar e corrigir para @carbon/icons-react
- Cherry-pick de commits de worktrees isoladas é o padrão correto; sem commit explícito no prompt do agent, o worktree é limpo automaticamente sem retornar mudanças
- Conflitos de ROADMAP.md são inevitáveis quando múltiplos agents auditam em paralelo — resolver manualmente mantendo HEAD + incorporando updates corretos
